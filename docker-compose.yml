services:
  api:
    image: ghcr.io/saleor/saleor:3.23.2
    ports:
      - "8000:8000"
    env_file:
      - .env
  worker:
    image: ghcr.io/saleor/saleor:3.23.2
    command: celery --app=saleor.celeryconf:app worker --loglevel=info
    env_file:
      - .env
  dashboard:
    image: ghcr.io/saleor/saleor-dashboard:3.23.2
    ports:
      - "9000:80"
    environment:
      API_URL: https://rahulsaleor.duckdns.org/graphql/
